# ocs2_legged_robot_adaptive_ros

Launch file:

- `launch/legged_robot_adaptive_ddp.launch`

The adaptive MPC node is:

- `legged_robot_adaptive_ddp_mpc`

It reuses the original dummy/target/gait nodes from `ocs2_legged_robot_ros`.
